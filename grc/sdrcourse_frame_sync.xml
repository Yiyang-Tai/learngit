<?xml version="1.0"?>
<block>
  <name>frame_sync</name>
  <key>sdrcourse_frame_sync</key>
  <category>[sdrcourse]</category>
  <import>import sdrcourse</import>
  <make>sdrcourse.frame_sync($preamble, $threshold)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>Preamble</name>
    <key>preamble</key>
    <type>int_vector</type>
  </param>
  <param>
    <name>Threshold</name>
    <key>threshold</key>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
  </source>

  <source>
    <name>out</name>
    <type>float</type>
  </source>
</block>
